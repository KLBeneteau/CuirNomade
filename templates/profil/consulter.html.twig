{% extends 'base.html.twig' %}

{% block body %}
    <div>Nom : {{ monProfil.nom }}</div>
    <div>Prenom : {{ monProfil.prenom }}</div>
    <div>Email : {{ monProfil.email }}</div>
    <div>Telephone : {{ monProfil.telephone }} </div>
    <a class="boutton" href="{{ path('profil_modifier') }}"> Modifier </a>
{% endblock %}

{% block title %}
    {{ monProfil.nom }} {{ monProfil.prenom }}
{% endblock %}

